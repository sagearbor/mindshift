// Jest stand-in for binary assets Metro would bundle (e.g. the .onnx model
// required by src/live/ortNative.ts). Mirrors what expo-asset's module
// registry returns for an asset: an opaque module id.
module.exports = 1;
